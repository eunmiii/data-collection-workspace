기본 용어 개념 -------------------------------------------------------------------------------------------------------------

크롤링
: URL을 수집하는 행위
- Selenium

크롤러 
: 위치(URL)을 탐색을 하는 프로그램. 
웹 상에서 자동으로 돌아다니며 링크를 타고 URL을 수집하고 저장하는 소프트웨어
- GoogleBot

스크래핑
: 발췌 행위
url 에서 특정 데이터를 추출하는 행위

파싱
: 가져온 데이터를 해석하고 데이터화하는 분석 과정
- BeautifulSoup



[모든 크롤링은 문자열로 데이터를 가져옴]

[크롤러]가 웹을 탐색하는 [크롤링]을 진행해 페이지를 찾으면, [스크래핑]을 통해 필요한 HTML을 가져온 뒤,
[파싱]을 통해 데이터 형태로 추출 및 정리를 한다.


통신 규약 -------------------------------------------------------------------------------------------------------------
웹문서 :  http://
도메인명 :  문자형태
포트번호 : 8080 등 (생략할수있는 번호 존재)
요청대상 :  url (페이지명) - uniform resource identify


GET / POST ------------------------------------------------------------------------------------------------------------
보안은 POST 방식으로. (노출 안됨)


JavaScript --------------------------------------------------------------------------------------------------------------
' .js '확장자로 저장
자바스크립트를 활용하여 만들어진 웹은 '동적 컨텐츠'이다.


속성 선택자 ------------------------------------------------------------------------------------------------------------
<img src="duke.png">
img[src=duke.png] {radius : 0.5;}


Ajax -------------------------------------------------------------------------------------------------------------------
비동기 통신 방식
Asynchronous JavaScript and XML의 약어 (But, 요즘은 XML 대신 JSON을 많이 사용함)

But, 요즘은 SPA(Single Page Application)을 사용하기도 함.


BeautifulSoup ----------------------------------------------------------------------------------------------------------
- uv add beautifulsoup4
- html 및 xml 파일에서 데이터를 추출하기 위한 파이썬 라이브러리

파서 종류 4가지
- html.parser
- lxml
- lxml-xml
- html5lib

데이터를 받아오는 능력은 없음.
파싱 역할임.


파싱 ---------------------------------------------------------------------------------------------------------------------
   - text
       - 문자열 형식으로 응답 컨텐츠 추출
       - 추출시 사용되는 문자셋은 'ISO-8859-1'이므로 'utf-8'이나 'euc-kr' 문자셋으로 작성된 콘텐츠 추출시 한글이 깨지는 현상 발생
       - 추출전 응답되는 컨텐츠의 문자셋정보를 파악하여 Response객체의 encoding속성에 문자 셋 정보를 설정한 후 추출

  - content
      - 바이트열 형식으로 응답 콘텐츠 추출
      - 응답 콘텐츠가 이미지와 같은 바이너리 형식은 경우 사용
      - 한글이 들어간 문자열 형식인 경우 r.content.decode('utf-8')를 사용해서 디코드 해야함


---------------------------------------------------------------------------------------------------------------

---------------------------------------------------------------------------------------------------------------


print(bs_obj.find('a').attrs ) 에서 attrs

string, text의 차이


---------------------------------------------------------------------------------------------------------------
true/ false 차이

uclass = bs_obj.find(class_='ko')
print(uclass)


print('----------------------------------------')
uclass = bs_obj.find(True, {'class':'ko'})
print(uclass)


print('----------------------------------------')
uclass = bs_obj.find(False, {'class':'ko'})                 # 유효하지 않은 태그
print(uclass)



siblings ---------------------------------------------------------------------------------------------------------------
요소 사이의 줄바꿈 한개도 이웃으로 인식을 함.
---------------------------------------------------------------------------------------------------------------
#sibling


print(bs_obj.find('a').next_siblings)       #generator object PageElement
ele = bs_obj.find('a').next_siblings
print(ele)


cnt = 0
for subNext in ele:
  cnt += 1
  print(subNext)


print('cnt', cnt)


---------------------------------------------------------------------------------------------------------------

#위와 동일한 값 출력
class = 'ko'으로 속성을 지정할 때 속성명이 class이면 반드시 class_로 해야 한다.
언더바 필수 ,,,,,,,,,!
ul_ko = bsobj.find('ul', class = 'ko')
print(ul_ko)


카카오에서 주소값 경도 위도 받아오기 ----------------------------------------------------------------------------------


---------------------------------------------------------------------------------------------------------------


---------------------------------------------------------------------------------------------------------------
